#include <stdio.h>

// 문자열(String) 이란 ?
// 문자열의 끝은 NULL 문자, 이진수 0, 문자열 종결자, ASCII 0, \0이라고 불리는
// 문자(char)은 1바이트
// 문자열(String)은 문자열의 길이 + 종결 문자를 더한 메모리값이 잡혀야한다.
// 예를 들면 "Hello, World !"라는 문자열의 길이는 14이지만 메모리는 15바이트가 잡혀야함.

// 배열(array)란?
// 동일한 자료형 변수들의 모임
// 문자 배열 문자들을 저장하는 변수들의 모임

int main()
{

  return 0;
}